- particles
  - blood (persistant!)
- graphics
  - beastmode bar (gets reset to max with every kill)
    - outer frame
    - colored inset that get's drawn up to current beastmode % (could be a color gradient for ex)
  - player
    - lunge stance, lunge charge, lunge end animation
    - beast mode anim
  - enemies, civilian walk cycles
  - level tiles (or non-tiles? maybe huge individual tiles that can be plugged together for a diverse level)
  - logo   -> name
  - menu background ???
